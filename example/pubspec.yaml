name: example
description: A new Flutter project.

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'
dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  gpt_markdown:
    path: ../../gpt_markdown
  flutter_math_fork:
    git:
      url: git@github.com:littlewrite/flutter_math.git
      ref: feat_more_paltform
  watcher: ^1.1.0
  desktop_drop: ^0.4.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
flutter:
  uses-material-design: true
